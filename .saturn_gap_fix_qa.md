# Saturn GAP Fix Final QA Report

Date: 2026-08-29 21:19 EEST
Repository: `/Users/ab/workspace/projects/saturn`

## Disposition

**FAIL (conditional / not release-green).** All GAP-focused and relevant existing tests pass, Spotless, package, and diff checks pass. The full Maven suite remains blocked by the explicitly protected, unrelated `AgentConfig` max-token mismatch: `OpenAiCompatibleClientTest.mapsOpenAiRequestAndToolCallResponseWithoutRequiringModel` expects 768 but observes 1024. Per scope, `AgentConfig.java` was not changed.

## Commands and exact outcomes

- `./mvnw -Dtest=AgentGapContractRedTest,AgentToolExecutionMiddlewareTest,AgentMessageProjectorTest,AgentToolExecutorTest,AgentToolCallSchedulerTest,AgentToolExecutionLedgerTest,AgentToolExecutionPolicyTest,AgentToolDescriptorTest,AgentRequestAssemblerTest,AgentPreparedRequestTest,DefaultAgentRouterTest,OpenAiCompatibleClientTest test`
  - **BUILD FAILURE only because of protected mismatch**.
  - 140 tests, 1 failure, 0 errors, 5 skipped.
  - GAP/relevant classes all passed; failing test: `OpenAiCompatibleClientTest.mapsOpenAiRequestAndToolCallResponseWithoutRequiringModel:87`, expected `<768>`, actual `<1024>`.
- `./mvnw -Dtest=AgentGapContractRedTest,AgentToolExecutionMiddlewareTest,AgentMessageProjectorTest,AgentToolExecutorTest,AgentToolCallSchedulerTest,AgentToolExecutionLedgerTest,AgentToolExecutionPolicyTest,AgentToolDescriptorTest,AgentRequestAssemblerTest,AgentPreparedRequestTest,DefaultAgentRouterTest test`
  - **BUILD SUCCESS**; 126 tests, 0 failures, 0 errors, 5 skipped.
- `./mvnw spotless:check`
  - **BUILD SUCCESS**; 0 files needing changes.
- `./mvnw test`
  - **BUILD FAILURE**; 624 tests, 1 failure, 0 errors, 5 skipped.
  - Sole failure is the protected `AgentConfig`/OpenAI max-token mismatch above.
- `./mvnw -DskipTests package`
  - **BUILD SUCCESS**; shaded artifact `/Users/ab/workspace/projects/saturn/target/saturn.jar` produced.
- `git diff --check`
  - **PASS**; no whitespace errors.
- `git status --short --untracked-files=all`
  - Reviewed. Existing unrelated runtime/IDE/config/database/analysis artifacts remain untouched and un-staged.

No JaCoCo HTML/CSV coverage report was present under `target` after these runs; coverage percentage is therefore not reported.

## Audit findings

- **API compatibility:** Existing `AgentToolExecutor` constructors and two-argument `executeAll` compatibility path remain present. `LlmRequest` compatibility constructors remain present; legacy requests remain projection-metadata empty.
- **Middleware production wiring:** `AgentRouterFactory` constructs immutable hooks once, and `DefaultAgentRouter` accepts and passes them to the request-local executor. The default observer is observational only; no gateway authorization bypass was introduced.
- **Exactly-once / observer behavior:** Focused middleware and executor tests pass, including one-shot continuation and observer exception isolation. Pre-validation outcomes remain outside the middleware chain, consistent with the narrow validated-invocation seam.
- **Batch context / cancellation:** Routed tool batches receive one request-derived deadline and fresh cancellation token. Execution context receives deadline/token. Explicit cancellation is now distinguished from deadline cancellation, including interruption while waiting on a cancelled future; exact-deadline expiry is handled as expired.
- **Scheduler/policy:** Scheduler compatibility checks delegate to `AgentToolExecutionPolicy`; contiguous provider order and conservative resource barriers remain intact. No per-call deadline authority remains in `AgentScheduledToolCall`.
- **Projection metadata/fingerprint:** `LlmRequest` validates projection fingerprint against its exact message list. Provider-facing router tool-turn/finalization requests use projected messages and metadata. Projector now copies message/tool-call list internals and safely handles non-system non-empty input without duplicating the tail. Existing projection tests pass.
- **Protected-file hygiene:** `AgentConfig.java` is still dirty from pre-existing work and was not edited. `config.toml`, `database/`, IDE/runtime artifacts, and copied analysis documents were not edited, reset, deleted, staged, or committed.

## Files modified by this QA pass

- `src/main/java/org/saturn/app/agent/tool/execution/AgentToolBatchContext.java`
  - Exact deadline expiry and cancellation-reason precedence.
- `src/main/java/org/saturn/app/agent/tool/execution/AgentToolCallScheduler.java`
  - Interrupted waits caused by batch cancellation/deadline now receive the batch result code rather than `TOOL_INTERRUPTED`.
- `src/main/java/org/saturn/app/agent/routing/AgentMessageProjector.java`
  - Defensive message copies and safe handling of non-system input.
- `src/main/java/org/saturn/app/agent/routing/DefaultAgentRouter.java`
  - Finalization provider request now carries projected metadata/fingerprint.

No commit or push was performed.

## Final QA conclusion

The GAP-focused implementation is test-green and packaging/formatting clean. The repository as a whole is **not green** solely because of the protected `AgentConfig` mismatch. That mismatch must be resolved by its owner before a full PASS can be issued; it was intentionally not repaired here.
